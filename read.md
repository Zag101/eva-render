# Eva Render

Endpoint HTTP per Eva (Second Life) ospitato su Render.

- `GET /eva?q=...` → restituisce testo semplice, pronto da mostrare in SL.
- Richiede la variabile di ambiente `OPENAI_API_KEY`.
